<h1 class="page-title reveal">The <em>chronicle</em></h1>
<p class="page-sub reveal">the realm's history, drawn from the action log · refreshed every 10 seconds</p>

<div id="chronicle" class="reveal" hx-get="/fragments/story" hx-trigger="every 10s" hx-swap="innerHTML">
<%= @body %>
</div>
